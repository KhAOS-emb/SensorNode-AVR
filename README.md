# SensorNode-AVR

ATmega328P sensor firmware with professional C++ architecture.

## Features
- Hardware Abstraction Layer (HAL)
- I2C, UART, GPIO, ADC, PWM drivers
- Unit-tested logic (GoogleTest)
- CI/CD via GitHub Actions

## Build Status
(CI badge kommt hier hin)
